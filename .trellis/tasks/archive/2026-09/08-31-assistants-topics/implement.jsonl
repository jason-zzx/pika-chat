{"file": ".trellis/spec/backend/directory-structure.md", "reason": "Three-layer boundary: (input, actor) service signatures with no Next.js types, four-line Route Handler shape, and the naming table this task deviates from twice (schema/assistant.ts over schema/topic.ts, two service files) — deviations are argued in design.md"}
{"file": ".trellis/spec/backend/database-guidelines.md", "reason": "UUIDv7 text ids, timestamptz, snake_case plural tables, ownership as a WHERE clause (topics reach it through assistants), and line 165's rule that cascading deletes on message data need an explicit product decision — D8 is that decision. D12 additionally makes assistant delete transactional with a FOR UPDATE lock, so read the query-building rules before writing it"}
{"file": ".trellis/spec/backend/error-handling.md", "reason": "AppError codes and the rule that a row failing the ownership filter is NOT_FOUND, never FORBIDDEN (AC2, AC8); duplicate assistant name maps to 409 via isUniqueViolation (AC5)"}
{"file": ".trellis/spec/backend/logging-guidelines.md", "reason": "Mutation logging shape; a system prompt is user content and must never reach a log line"}
{"file": ".trellis/spec/frontend/directory-structure.md", "reason": "components/ui/ is generated territory — the three new primitives must stay unedited (AC16); domain folders for components/assistant and components/topic; hook and API client naming"}
{"file": ".trellis/spec/frontend/component-guidelines.md", "reason": "Server/client boundary (AppSidebar stays a Server Component, only AssistantTree is client), one tree at both breakpoints, and loading/empty/error states as first-class deliverables (fixed-width Skeleton, per-assistant empty state)"}
{"file": ".trellis/spec/frontend/state-management.md", "reason": "Default the open pane from useParams; the Back override is local useState, not Zustand (AC12); server state is TanStack Query only, src/stores/ must not appear"}
{"file": ".trellis/spec/frontend/hook-guidelines.md", "reason": "Query key factory and mutation-then-invalidate conventions for use-assistants.ts and use-available-models.ts"}
{"file": ".trellis/spec/frontend/type-safety.md", "reason": "Shared Zod contracts between client and Route Handlers. Note: its topicSchema example at line 24 declares assistantId nullable, which D1 overrides — implement NOT NULL and leave the spec fix to Phase 3.3"}
{"file": ".trellis/spec/guides/code-reuse-thinking-guide.md", "reason": "Reuse before writing: SidebarNavLink (mobile drawer closing), EmptyState, PageContainer/PageHeader, parseJson/parseEmpty in lib/api, the grouped-Map read shape in provider.service.ts, and resolveAvailableModels rather than a second availability query (AC6)"}
{"file": ".trellis/spec/guides/cross-layer-thinking-guide.md", "reason": "This task spans schema, migration, service, transport, client API, hooks, and shell UI in one change; use it to keep the layers consistent"}
